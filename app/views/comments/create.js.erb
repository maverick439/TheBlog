$('#comments_count').html("<%= escape_javascript(pluralize(@article.comments.count, 'Comment')) %>");

$('#article_comment').prepend("<%= escape_javascript(render partial: 'articles/comment', locals: {article: @article, comment: @comment}) %>");

$('#comment_description').val('');


